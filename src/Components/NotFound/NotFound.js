/** @format */

import React from "react";
import { useStyles } from "./style";

export default function NotFoundPage() {
  const classes = useStyles(); //className={classes.notFound}
  return <h1>Page Not Found</h1>;
}
