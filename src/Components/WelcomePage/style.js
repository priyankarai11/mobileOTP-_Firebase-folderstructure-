/** @format */

import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
  home: {
    display: "flex",
  },

  homeContainer: {
    flex: 5,
    marginTop: 40,
  },

  widgets: {
    display: "flex",
    padding: 20,
    gap: 20,
  },
});
